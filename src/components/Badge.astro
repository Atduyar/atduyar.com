---
interface Props {
	class?: string;
}

const { class: className = "" } = Astro.props;
---
<span class={`text-sm px-3 py-1.5 border border-white/10 hover:border-blue-300/50 hover:bg-blue-300/10 transition-colors ${className}`}>
	<slot/>
</span>
